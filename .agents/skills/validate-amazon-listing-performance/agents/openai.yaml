interface:
  display_name: "Amazon Keyword Position and CPC Validation"
  short_description: "Validate all top-3-page organic terms, exact ads, CPC, and auto terms"
  default_prompt: "Use $validate-amazon-listing-performance for this target ASIN, read the product and test-advertising inputs from the project Feishu sheet, capture every target-family organic keyword in pages 1-3 with immutable first-detected times, and publish the verified keyword result into a suitable current-date keyword column or append the paired date columns when needed."

policy:
  allow_implicit_invocation: true
